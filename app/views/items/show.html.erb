<div class= "colorevent container col-10">
 <div class="row mb-3">
   <div class="col-md-8 themed-grid-col">
     <div class="block-image">
        <img class="img-thumbnail"><%= @item.image_url%>
   </div>
</div>

<div class="col-6 col-md-4 mb-4 themed-grid-col">
    <h5 class="card-title"><%= @item.title %></h5>
    <p class="card-text"><%= @item.description%></p>
    <p class="card-price"><%= @item.price%>€</p>
    <button type="button" class="btn btn-outline-primary"><%= link_to "Ajouter au panier", items_path(@item.id), method: 'POST' %></button>
   </div>
 </div>
</div>